# Same Supabase project as frontend/ — anon key only, protected by
# Row Level Security. Admin access is enforced by the admins table +
# RLS policies, not by this key being secret.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — powers the "View site" link in the sidebar.
VITE_SITE_URL=http://localhost:5173
